import { NombreAlumnosPipe } from './nombre-alumnos.pipe';

describe('NombreAlumnosPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreAlumnosPipe();
    expect(pipe).toBeTruthy();
  });
});
