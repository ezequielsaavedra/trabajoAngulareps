import { NombreProfesoresPipe } from './nombre-profesores.pipe';

describe('NombreProfesoresPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreProfesoresPipe();
    expect(pipe).toBeTruthy();
  });
});
